export default interface User {
    _id: string;
    username: string;
    email: string;
    avatar: string;
    createdAt: string;
    updatedAt: string;
    lastMessage: string;
    conversationId: string;
}